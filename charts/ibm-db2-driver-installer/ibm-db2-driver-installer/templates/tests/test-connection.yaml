apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ibm-db2-driver-installer.fullname" . }}-test-connection"
  labels:
{{ include "ibm-db2-driver-installer.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ibm-db2-driver-installer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
