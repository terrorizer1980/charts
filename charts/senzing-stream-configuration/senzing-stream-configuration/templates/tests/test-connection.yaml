apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "senzing-stream-configuration.fullname" . }}-test-connection"
  labels:
{{ include "senzing-stream-configuration.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "senzing-stream-configuration.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
