apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "senzing-entity-search-web-app.fullname" . }}-test-connection"
  labels:
{{ include "senzing-entity-search-web-app.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "senzing-entity-search-web-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
