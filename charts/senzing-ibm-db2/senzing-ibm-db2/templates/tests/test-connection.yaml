apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "senzing-ibm-db2.fullname" . }}-test-connection"
  labels:
{{ include "senzing-ibm-db2.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "senzing-ibm-db2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
