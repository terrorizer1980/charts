apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "senzing-package.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "senzing-package.name" . }}
    helm.sh/chart: {{ include "senzing-package.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "senzing-package.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
