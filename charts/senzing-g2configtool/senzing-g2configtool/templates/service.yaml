apiVersion: v1
kind: Service
metadata:
  name: {{ include "senzing-g2configtool.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "senzing-g2configtool.name" . }}
    helm.sh/chart: {{ include "senzing-g2configtool.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "senzing-g2configtool.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
