apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "senzing-init-container.fullname" . }}-test-connection"
  labels:
{{ include "senzing-init-container.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "senzing-init-container.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
